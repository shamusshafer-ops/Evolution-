{
  "name": "evolution-simulator",
  "version": "1.0.0",
  "private": true,
  "description": "A seeded natural-selection simulator",
  "scripts": {
    "build": "node build.js",
    "check": "node build.js --check"
  },
  "dependencies": {
    "three": "0.185.1"
  },
  "devDependencies": {
    "esbuild": "0.28.1"
  }
}
